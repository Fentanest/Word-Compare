[package]
name = "word_compare_native_extractor"
version = "0.1.0"
edition = "2021"

[dependencies]
roxmltree = "0.20"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
zip = { version = "2", default-features = false, features = ["deflate"] }
